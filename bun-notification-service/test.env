# Test env — nama env var SAMA PERSIS dengan docker-compose.dev.yml
# FIREBASE_CREDENTIALS_JSON hanya untuk testing/CI (production baca dari file mount)
REDIS_URL=redis://localhost:6379
DATABASE_URL=postgresql://localhost:5432/test
FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"test-project"}
EMAIL_FROM=noreply@rejki.id
# SMTP opsional — tidak di-set di test (verify undefined)
